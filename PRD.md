# WeRead AI 产品说明 / PRD

## 1. 产品概述

WeRead AI 是一个嵌入微信读书阅读页的 AI 阅读记忆工具，帮助用户在长篇阅读中保持进度、快速回顾和沉淀划线笔记。

## 2. 目标用户

需要持续阅读小说、非虚构书籍或专业资料，并希望把阅读过程转化为可回顾知识的人。

## 3. 用户痛点

1. 重新打开书时，想不起上次读到哪里。
2. 章节多、情节复杂时，很难快速找到关键转折。
3. 划线散落在阅读平台里，难以整理到自己的知识库。

## 4. MVP 范围

### 必须有

- 在阅读页展示 AI 侧边栏。
- 根据章节目录生成 5–10 个关键情节节点。
- 保存阅读进度并生成“上次读到哪里”的回顾。
- 支持手动编辑节点，避免 AI 判断不可修改。
- 将划线整理为 Markdown，导出到 Obsidian。

### 暂不做

- 多用户账号体系和云端同步。
- 公开部署后的跨设备数据同步。
- 对微信读书所有页面结构的长期兼容保证。
- 自动理解整本书全文内容；当前主要使用章节目录、阅读记录和用户划线。

## 5. 核心用户流程

```text
打开微信读书
  → Chrome 扩展识别书籍
  → 读取章节目录并请求本地 API
  → 生成进度条关键节点
  → 用户阅读并自动保存进度
  → 下次打开时查看阅读回顾
  → 用户确认/编辑节点
  → 将划线或总结导出到 Obsidian
```

## 6. 产品决策

- AI 输出必须可编辑：关键节点不是不可质疑的最终答案。
- 默认本地运行：Obsidian 路径和 API Key 属于用户本机数据，不直接交给公共服务。
- 先解决“回到阅读现场”而不是做通用读书社区：MVP 聚焦进度、回顾和沉淀。
- 失败要可理解：API 未启动、没有阅读记录、没有划线时，界面给出下一步提示。

## 7. 验证指标

- 新用户能否在 1 分钟内理解侧边栏三个主要功能。
- 章节节点是否能帮助用户快速定位关键段落。
- 用户是否能在不修改代码的情况下编辑节点和配置导出路径。
- AI 服务不可用时，扩展是否仍能保留本地节点和阅读进度。

